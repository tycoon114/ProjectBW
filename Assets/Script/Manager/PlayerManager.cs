using UnityEngine;

public class PlayerManager : MonoBehaviour
{
   

    /*
        작동 방식 설계
     체력, 공격력 등의 정보는 따로 받아온다.
    캐릭터가 생성 될때 받아올것 (현재 테스트 씬에서는 생략), 캐릭터 명을 대조하여 해당하는 정보를 받아옴
    컬럼 : 캐릭터명, 공격력, 체력
    피격을 받으면 이 스크립트를 호출?
     
     */

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
